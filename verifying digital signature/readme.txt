TestCase1:
	Hash of the document is to identify the document.
	Signing the document with user provate key via remix to make sure the identity of the documnet towards the user.
	REASON:To make sure every document has unique ID. The change in document changes the hash(ID).
testcase2:
	Remove the hash of the document with specified user signature and make sure wrong user is not deleted.
TestCase3
	getting the sign of the document.
	REASON: Make sure that document has been signed by the correct owner.